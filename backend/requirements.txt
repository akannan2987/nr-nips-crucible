# Crucible Python backend — runtime dependencies
# Install into a virtualenv:  python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
SQLAlchemy>=2.0,<3.0
alembic>=1.13                  # database schema migrations (SQLite + PostgreSQL)
psycopg[binary]>=3.1           # PostgreSQL driver (DATABASE_URL=postgresql+psycopg://...)
pydantic>=2.7,<3.0
python-multipart>=0.0.9        # required by FastAPI for multipart file uploads
pandas>=2.2
openpyxl>=3.1
rdkit>=2024.3.1,<2025.9.4      # newest release with pre-built wheels for EVERY machine this project uses:
                               # Linux x86_64 (image, VM), macOS arm64 (CI), macOS Intel (the development
                               # laptop), Python 3.12-3.14. Before lifting the cap, check the next version
                               # with: pip download --only-binary=:all: --no-deps rdkit==<v>  on each of them.

# Test dependencies
pytest>=8.0
httpx>=0.27                    # used by fastapi.testclient and the live parity diff
ruff>=0.6                      # linter; the same check CI runs
