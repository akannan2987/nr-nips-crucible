# Moved

This document now lives at [docs/02-database-schema.md](02-database-schema.md). This one-line file stays for one release so that old links and bookmarks keep working; it will be removed in the release after next.
